# Internal Used Vehicle Pricing and Inventory Optimization Agent
# Prototype dependencies.

# Schema validation (scripts/validate_schemas.py, §28 step 9)
jsonschema>=4.21
referencing>=0.33

# Config
PyYAML>=6.0

# Deterministic and simulation layers
numpy>=1.26
pandas>=2.2

# UI
streamlit>=1.33
plotly>=5.20

# LLM orchestration and explanation layer.
# Never imported by src/pricing_agent/domain or /simulation -- enforced by
# tests/unit/test_architecture.py.
anthropic>=0.40

# Tests
pytest>=8.0
